"use client";import {useState} from "react";import {stages} from "@/lib/data";export function LeakMap(){const [active,setActive]=useState(0),s=stages[active];return <section className="section"><div className="container"><p className="eyebrow">From symptoms to causes</p><h2>We Find the Problem Behind the Problem.</h2><div className="leak"><div role="tablist">{stages.map((x,i)=><button role="tab" aria-selected={active===i} onClick={()=>setActive(i)} key={x.name}><x.Icon/><span><small>0{i+1}</small>{x.name}</span></button>)}</div><article><s.Icon/><h3>{s.name}</h3><small>The problem</small><p>{s.problem}</p><small>How Sage Partners helps</small><p>{s.help}</p><a className="btn dark" href="#contact">Diagnose this stage</a></article></div></div></section>}
