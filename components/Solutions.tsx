"use client";import {useState} from "react";import {ChevronDown} from "lucide-react";import {solutions} from "@/lib/data";const steps=[["Diagnose","We examine the complete customer journey, not just the visible page."],["Prioritize","We separate urgent revenue leaks from low-impact distractions."],["Implement","Our specialists improve the areas preventing profitable growth."],["Measure","We compare changes against the starting point and identify what should happen next."]];export function Solutions(){const [open,setOpen]=useState(0);return <><section id="help" className="section tint"><div className="container"><p className="eyebrow">A practical working method</p><h2>From “Something Is Wrong” to a Clear Plan.</h2><div className="steps">{steps.map(([n,d],i)=><article key={n}><small>0{i+1}</small><h3>{n}</h3><p>{d}</p></article>)}</div><aside className="paper">“You will always know what we are changing, why it matters and what should happen next.”</aside></div></section><section className="section alt"><div className="container"><p className="eyebrow">Problem-matched capabilities</p><h2>The Right Help for the Problem in Front of You.</h2><div className="accordions">{solutions.map(([n,p],i)=><article key={n}><button onClick={()=>setOpen(open===i?-1:i)} aria-expanded={open===i}><span><small>{String(i+1).padStart(2,"0")}</small>{n}</span><ChevronDown/></button>{open===i&&<div><b>Problem solved</b><p>{p}</p><p>We examine the customer journey, content, technical setup and performance signals connected to this problem, then complete the highest-impact practical fixes.</p><a href="#contact">Ask about this problem →</a></div>}</article>)}</div></div></section></>}
