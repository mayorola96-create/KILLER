"use client";import Image from "next/image";import {ArrowUpRight,Menu,X} from "lucide-react";import {useEffect,useRef,useState} from "react";const links=[["Problems","problems"],["How We Help","help"],["AISEO","aiseo"],["Transformations","transformations"],["Team","team"],["Contact","contact"]] as const;export function Header(){const [open,setOpen]=useState(false),panel=useRef<HTMLDivElement>(null),trigger=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;const fs=panel.current?.querySelectorAll<HTMLElement>("a,button");fs?.[0]?.focus();const key=(e:KeyboardEvent)=>{if(e.key==="Escape"){setOpen(false);trigger.current?.focus()}if(e.key==="Tab"&&fs?.length){const f=fs[0],l=fs[fs.length-1];if(e.shiftKey&&document.activeElement===f){e.preventDefault();l.focus()}else if(!e.shiftKey&&document.activeElement===l){e.preventDefault();f.focus()}}};document.addEventListener("keydown",key);document.body.style.overflow="hidden";return()=>{document.removeEventListener("keydown",key);document.body.style.overflow=""}},[open]);return <header><div className="container nav"><a href="#main" aria-label="Sage Partners home"><Image src="https://cdn.phototourl.com/free/2026-07-18-361e55d7-48ce-4924-88c3-6e94930fbdd0.png" alt="Sage Partners" width={170} height={60} priority/></a><nav className="desktop">{links.map(([l,id])=><a key={id} href={`#${id}`}>{l}</a>)}<a className="btn dark" href="#contact">Diagnose My Store <ArrowUpRight size={17}/></a></nav><button ref={trigger} className="menu" onClick={()=>setOpen(true)} aria-label="Open menu" aria-expanded={open}><Menu/></button></div>{open&&<div className="backdrop"><div ref={panel} className="drawer" role="dialog" aria-modal="true" aria-label="Navigation"><button onClick={()=>setOpen(false)} aria-label="Close menu"><X/></button>{links.map(([l,id])=><a onClick={()=>setOpen(false)} key={id} href={`#${id}`}>{l}</a>)}<a className="btn dark" href="#contact">Diagnose My Store</a></div></div>}</header>}
